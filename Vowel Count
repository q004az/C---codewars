size_t get_count(const char *s)
{
    int sum =0;
    int len = strlen(s);
    for (int i = 0; i < len; i++)
      if (s[i] == 'a'|| s[i] == 'e' || s[i] == 'i' || s[i] == 'o' || s[i] == 'u')
        sum+=1;
    return sum;
}
